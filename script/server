#! /usr/bin/env elixir

Mix.install([
  {:rot13, path: "./"},
  {:norm, "~> 0.13.0"}
  ]
  )

command_line = CommandLine.create()
server = Server.create(command_line: command_line)
Server.run(server)
Process.sleep(:infinity)
